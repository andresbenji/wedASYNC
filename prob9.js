const divisableByTen = (number) => number % 10 === 0;

//if the inputted number has a remainder of 0 when divided by 10, then the output will be true, otherwise it will be false.

console.log(divisableByTen(13)); //false

console.log(divisableByTen(50)); //true
